import styled from 'styled-components';

const Wrapper = styled.div`
  margin: 6px 0 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
`;

const P = {
  Wrapper,
};

export default P;
